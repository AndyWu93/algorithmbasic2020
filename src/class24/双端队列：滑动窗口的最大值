双端队列：滑动窗口的最大值/最小值

有一个数组arr，在一个滑动窗口中，求任何时候的最大值
滑动窗口：
窗口的L,R只能向右滑动，任意时刻窗口的大小动态改变

准备一个双端队列q：队列中只放index，代表的数从前往后严格由大到小
当R++，窗口中进入一个数x，比对q尾部代表的数是不是比x小，小的话就弹出，看新尾巴，直到这个数比x大，把x放到q的尾部
当L++，窗口中出去一个数y, 看q中头部的index是不是出去的数的index，是的话表示过期，弹出头部

双端队列q的含义：
如果此时开始缩小窗口，哪些数会依次成为窗口中的max
复杂度：arr中所以的数最多进一次q出一次q：O(N)
