红黑树
也是一种模糊平衡性的BST
特点：
    每个节点，不是红就是黑
    整棵树的头节点、叶节点（就是null节点）是黑
    任意两个红节点不能相邻
    对于任意一棵子树中，从头节点开始的任意一条链中黑节点的数量必须相等
平衡性：
    任意一个节点的为头节点的子树中，最长链表（红黑相间） <= 最短链表（全黑） * 2
调平策略：
    插入之后5种违规状态
    删除之后8种违规状态
    一共13种


有序表解题核心：
1）分析增加什么数据项可以支持题目
2）有序表一定要保持内部参与排序的key不重复
3）增加这个数据项了，在平衡性调整时，保证这个数据项也能更新正确
4）做到上面3点，剩下就是搜索二叉树怎么实现你想要的接口的问题了
