二叉树遍历序面试题
证明：
二叉树中某一个节点x，先序遍历时x左边的集合记为A，后序遍历时x右边的集合记为B，A∩B为x的所有祖先节点。

对于节点x一共有5类节点：
1. x所有的祖先节点
2. x自己
3. x所有的子孩子节点
4. x为左树时，所有的右树兄弟节点
5. x为右树时，所有的左树兄弟节点

先序：头左右
后序：左右头

分析上面5类节点，排除AB集合中的345，2原本就不在集合中，只剩下1
1. x所有的祖先节点肯定在先序时x的左边，后序时x的右边
2. x所有的子孩子节点，出现在先序遍历时x的右边
3. x为左树时，所有右树兄弟节点，出现在先序遍历时x的右边
4. x为右树时，所有的左树兄弟节点，出现在后序遍历时x的左边